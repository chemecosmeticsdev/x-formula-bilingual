{
  "name": "formula-platform-image-generation",
  "version": "1.0.0",
  "description": "AWS Lambda function for generating product packaging images using Bedrock",
  "main": "src/handler.js",
  "scripts": {
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod",
    "offline": "serverless offline",
    "test": "jest",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.658.1",
    "@aws-sdk/client-s3": "^3.658.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-offline": "^13.8.0",
    "jest": "^29.7.0",
    "eslint": "^8.57.1"
  },
  "author": "Formula Platform Team",
  "license": "MIT"
}
